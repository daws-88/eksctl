apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: roboshop-pratice
  region: us-east-1
  
managedNodeGroups:
  - name: roboshop-dev
    instanceTypes: "t3.micro"
    desiredCapacity: 2 # by default 3
    spot: true
    